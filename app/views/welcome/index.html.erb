<div class="row">
  <%#= @homeworks.length %>
  <% (0...@homeworks.length).each do |index| %>
      <%= render partial: "homework", locals: { subject: @homeworks[index], teacher: @teachers[index], content: @hwcontent[index] } %>
  <%end%>
</div>
